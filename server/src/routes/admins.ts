import type { FastifyInstance, FastifyRequest, FastifyReply } from "fastify";

const routes = async (app: FastifyInstance) => {
  // TODO: create admin control api routes
}

export { routes as admin_routes };
